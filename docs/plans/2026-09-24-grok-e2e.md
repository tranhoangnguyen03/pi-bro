# Grok manual E2E — capability-first revision

Back up `${PI_CODING_AGENT_DIR:-$HOME/.pi/agent}/bro-settings.json`.

```sh
BRO="$HOME/Github/pi-bro/.worktrees/backend-grok"
TEST="$(mktemp -d /tmp/bro-grok-e2e.XXXXXX)"
printf 'GROK_WORKSPACE_CANARY_8194\n' > "$TEST/sample.txt"
cd "$TEST"
pi --no-extensions -e "$BRO/bro.ts" --no-skills --no-prompt-templates --no-context-files --session "$TEST/session.jsonl"
```

1. `/bro config`: choose Grok grok-4.7-build-fast as shared default, default or low effort. All four capabilities should be supported. Verify custom model selection, Esc cancellation, pinned overrides, effort resets across backend switches and persistence.
2. `/bro text There are two retries, after five and ten seconds.` Expect streamed explanation preserving facts. R reruns, C copies, Esc closes, `/bro open` reopens without executing again. `/bro file sample.txt` uses extracted supplied text.
3. After a parent conversation, `/bro show 2`: expect visualization based on that transcript, not unsolicited repository research. This is requested behavior, not technically enforced isolation.
4. `/bro btw --fresh Remember the token BLUEBIRD_314.` Then ask for the token. Close/reopen and ask again: native continuation should retain it. `/clear` starts fresh. No side output enters parent conversation without explicit insertion.
5. `/bro btw --full Read sample.txt and report its contents; do not modify files.` Expect actual workspace read. Ask a follow-up to verify native continuation. Switching `--full`/`--sandbox` starts a fresh thread; The modal header shows the selected model and effort, without access-policy labels.
6. During a Grok BTW thread switch BTW override to Agy, then ask again. Expect visible fresh-thread reset, never Grok ID passed to Agy. Switch back to Grok and verify fresh start. Claude BTW remains unsupported pending #58.
7. Set `/bro advisor-steer` to `Begin with STEER_2801. Do not modify files.` Ask parent to call bro_advisor to inspect sample.txt. Expect tool activity, token and steering prefix, Grok label. Advisor is fresh each time.
8. Cancel explain/show/BTW/advisor mid-run. No late successful result or cancellation retry; subsequent calls remain usable. Process-group cleanup allows five seconds for stubborn children, not containment of detached/external work.
9. `/bro doctor`: effective selections and version without claiming auth/connectivity. `/bro model` custom IDs, `/bro effort` default/low/medium/high/xhigh. Model-specific unsupported effort produces CLI diagnostic without clamping. `/bro usage` has been removed; research/reimplementation is tracked in #61. Explain/show/BTW headers display model and effort; `/bro open` preserves the completed answer’s label.

## Actual access and retention

Grok always has its normal workspace/tool authority with explicit sandbox off and bypassPermissions. Explain/show use scratch cwd; ordinary BTW uses stable workspace cwd with prompt instructions to stay within supplied context. These instructions are not enforced isolation. Full BTW invites workspace access; advisor uses it. Native tools are not blacklisted merely for being powerful. Hooks/skills/plugins/MCP may load. Bro removes its private prompt file, while Grok may persist native sessions/logs. No credentials are copied and no Grok settings are rewritten.
